import ProfilePreferences from '@/components/forms/profile-preferences';

export default {
  title: 'forms/profile-preferences',
  component: ProfilePreferences,
};

export const Template = () => {
  return (
    <div>
      <ProfilePreferences />
    </div>
  );
};
